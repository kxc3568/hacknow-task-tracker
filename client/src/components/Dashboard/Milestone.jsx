import React, { Component } from 'react';
import styles from '../../styles/dashboard/milestone.module.css';

class Milestone extends Component {
    state = {  }
    render() { 
        return ( <div className={styles.container}>
            
        </div> );
    }
}
 
export default Milestone;